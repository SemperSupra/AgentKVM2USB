# Line-ending policy for the reverse-engineering toolchain.
# Shell scripts must be checked out as LF so WSL `bash -n` and `bash -lc`
# accept them without CRLF (`do\r`) parse errors.
*.sh text eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.yml text eol=lf
*.yaml text eol=lf
